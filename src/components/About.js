import React, {Component} from 'react'

class About extends Component {
    render () {
        return (
            <div>This is an intelligent game built on React. Here, user plays against computer to grab islands with the maximum area. The only rule is, selection at any point during the game play, could be made from either of available ends. One who grabs more area will win the game.</div>
        );
    }
}

export default About;